{#
/**
 * @file
 * Default theme implementation for a single field in a view.
 *
 * Available variables:
 * - view: The view that the field belongs to.
 * - field: The field handler that can process the input.
 * - row: The raw result of the database query that generated this field.
 * - output: The processed output that will normally be used.
 *
 * When fetching output from the row this construct should be used:
 * data = row[field.field_alias]
 *
 * The above will guarantee that you'll always get the correct data, regardless
 * of any changes in the aliasing that might happen if the view is modified.
 *
 * @see template_preprocess_views_view_field()
 *
 * @ingroup themeable
 */
#}

<style>
  #page #header{
    display: none;
  }

  body {
    background-image: url(/themes/custom/koala/images/Group463.png);
    background: #ff92941c;
  }

  #page #main-wrapper {
    margin-top: 0 !important;
  }
  footer.site-footer {
    display: none;
  }
  iframe {
    height: 88vh;
    background-image: url(/themes/custom/koala/images/Group463.png);
  }

  body #main-wrapper #main.container {
    padding: 0;
    max-width: 98% !important;
    width: 98% !important;
  }

  .acciones-geneally {
    gap: 1rem;
    padding-top: 1rem;
    padding-bottom: 1rem;
    display: flex;
    flex-direction: row;
    flex-wrap: nowrap;
    align-content: center;
    justify-content: center;
    align-items: center;
  }
  .codigo {
    font-weight: 700;
  }
  .acciones-geneally a {
    border: 2px solid var(--turq-k-500, #297e88);
    background: var(--white, #fff);
    padding: 0.5rem 1rem;
    border-radius: 32px;
    font-size: 0.9rem;
    color: black;
    text-decoration: none;
  }
  .actividades.extra {
    display: flex;
    flex-direction: row;
    flex-wrap: nowrap;
    gap: 1rem;
  }

</style>
<div class="geneally-show">
  <iframe src="{{ output -}}" width="100%" height="100%" frameborder="0" scrolling="no" id="geneally-show-iframe" name="geneally-show-iframe"></iframe>
</div>